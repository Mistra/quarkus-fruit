quarkus:
  datasource:
    db-kind: h2
    username: root
    jdbc:
      url: jdbc:h2:mem:test
  hibernate-orm:
    database:
      generation: drop-and-create
    log:
      sql: false
    sql-load-script: import.sql